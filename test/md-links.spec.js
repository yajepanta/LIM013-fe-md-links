const mdLinks = require('../');


describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

});

describe('initial', () => {
  test('first tests', () => {
    expect(true).toBe(true)
  })
});
