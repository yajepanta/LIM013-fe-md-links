let animales = ["gato", "perro", "pollo"];

/* for each */
/* recorre pero no se almacena, y no retorna */

animales.forEach( () => {
    
});
/* map */
/* recorre y devuelve un nuevo array, retorna el resultado de una función */


animales.forEach((x)=>{ console.log(x == "rana");});

const ejemplo = animales.map((x) => x == "rana");

ejemplo

Promesa1
.then( )
.catch();

const prom = new Promise;
Promise 
const num = [1, 3, 5];

(number) => { for (let i; i++; i <(number.length)-1) {
    number 
}}; 